#pragma once

// Private stb_image header (declarations only).
// The implementation is defined exclusively in stb_image_loader.cpp
// to avoid ODR violations across translation units.

#include "stb_image.h"
